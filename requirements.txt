# DoxifySlim 依赖（无本地模型、无 GPU 依赖，全部有 Windows / macOS / Linux 预编译轮子）
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0
PyMuPDF>=1.24.0
langdetect>=1.0.9
# FastAPI 表单/文件上传解析
python-multipart>=0.0.9
# DOCX 翻译：原位改 XML 文本节点
lxml>=5.0.0
# 修复 LLM 返回的不规范 JSON（未转义引号、尾逗号等）
json-repair>=0.30.0
# 测试
pytest>=8.0.0
# 测试用：验证翻译后的 .docx 能被 Word 系工具打开（引擎本身只依赖 lxml）
python-docx>=1.1.0
